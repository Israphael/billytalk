# СТАРТ ЗДЕСЬ — точка входа для любого инструмента и любой сессии

Этот файл — карта проекта, самодостаточная и в самом репозитории (в отличие от
`HANDOFF` на рабочем столе и памяти Claude Code — они специфичны для конкретной
установки и в другой инструмент не переезжают). Кто бы ни открыл `C:\BillyTalk`
следующим — Claude Code, BillyGPT, другой агент, живой человек — читать отсюда.

## Где мы (на 23.07.2026)

**MVP-0 по коду готов и отревьюен. Заказчик пользуется программой каждый день,
всё устраивает.** Идёт последний, немашинный критерий: неделя реальной работы.

- Диктовка под Windows: удержал кнопку → сказал → отпустил → текст в поле.
  Расшифровка — Groq (`whisper-large-v3-turbo`), сырая, без причёсывания.
- Готовый установщик: `dist/BillyTalk-Setup.exe` (NSIS, ~25 МБ) и копия в
  Загрузках заказчика. Собирается из `packaging/installer.nsi`.
- **572 теста зелёные.** `.venv` (Python 3.14): `.venv/Scripts/python.exe -m pytest -q`.

## ⚠️ Состояние git — читать первым

- Ветка с последней работой: **`feat/cycle3-packaging`** (весь цикл 3).
- Веха 3 цикла 2: **`feat/cycle2-m3`**.
- **Обе ветки ТОЛЬКО локальные. Ничего не запушено.** `origin/main` на GitHub —
  `a2d3b8e`, месячной давности (до вехи 3). Локально `main` там же.
- **38 коммитов цикла 3 существуют только на этой машине.** Перед работой из
  другого окружения их надо запушить — но **push и слияние в `main` решает
  только заказчик** (правило проекта, не нарушать).

## Что читать по порядку

1. `docs/ru/00-СОСТОЯНИЕ-ПРОЕКТА.md` — развёрнутое состояние, история циклов.
2. `docs/ru/spec/02-спецификация-MVP-0.md` — спецификация (§8 доставка, §11
   интерфейс, §12 установщик/мастер, §13 приватность).
3. `docs/ru/spec/OPEN-QUESTIONS.md` — **40 записей**, каждая = принятое прочтение
   двусмысленности. Не переоткрывать, пополнять. Свежие: §37 (проба микрофона),
   §38 (WAL-очистка), §39 (поток дорожки повторов), §40 (сигнал в терминале).
4. `docs/ru/ПРИЁМКА-MVP-0.md` — лист приёмки §16: каждый пункт с методом
   проверки и отметкой авто/живьём.
5. `docs/ru/ОЧЕРЕДЬ-ИЗМЕНЕНИЙ.md` — заявки на следующую версию (звуки,
   проверка обновлений) + **решение: подпись кода НЕ делаем**.
6. `docs/ru/spec/03-harness-слой-для-сборки.md` — раскладка, протокол IPC, §13.

## Что сделано в цикле 3 (поставка)

Мастер первого запуска §12 (7 шагов), локаль ru/en (`billytalk/i18n/`),
уведомления через трей, автозапуск §12, иконка и картинка установщика
(генерируются кодом, сверяются тестом), NSIS-установщик с честным обновлением
поверх и удалением. **Три ревью (адверсариальное + security + ревью
установщика) отработаны, все находки починены с регресс-тестами.** Плюс первый
живой баг от заказчика: git bash (mintty) брал не тот аккорд вставки —
`Shift+Insert`, измерено (OQ §40).

## Решения, которые НЕ переоткрывать

- **Подпись кода — не делаем** (`ОЧЕРЕДЬ-ИЗМЕНЕНИЙ.md`): сотрудникам довольно
  одного клика на SmartScreen; рос. документы + Бразилия делают валидацию
  морокой.
- **Причёсывание (`polish_enabled`) выключено** и заказчику пока не нужно —
  всё, что он хвалит, это сырой Groq.
- Машину состояний не переписывать. Вставку исполняет ЯДРО по сохранённому
  HWND. Ключ Groq — только в диспетчере учётных данных. В логи никогда: аудио,
  текст расшифровки, заголовки окон, буфер, нажатия сверх хоткея.

## Что дальше

1. Неделя реального использования (идёт) — финальный критерий MVP-0.
2. Решение заказчика: слить `feat/cycle2-m3` и `feat/cycle3-packaging` в `main`
   + push.
3. Следующая версия — по `ОЧЕРЕДЬ-ИЗМЕНЕНИЙ.md`.

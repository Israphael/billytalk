# Pinned to the versions verified by installing and running on Python 3.14 /
# win_amd64 (ADR-0001), not by reading a compatibility table.
#
#   uv venv --python 3.14 .venv
#   uv pip install -r requirements.txt

wxPython==4.2.5
sounddevice==0.5.5
soundfile==0.14.0
numpy==2.5.1
pywin32==312
comtypes==1.4.16

# Development and packaging
pytest>=8
hypothesis>=6
pyinstaller==6.21
